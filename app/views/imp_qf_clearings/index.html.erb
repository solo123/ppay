<p id="notice"><%= notice %></p>

<h1>Listing Imp Qf Clearings</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @imp_qf_clearings.each do |imp_qf_clearing| %>
      <tr>
        <td><%= link_to 'Show', imp_qf_clearing %></td>
        <td><%= link_to 'Edit', edit_imp_qf_clearing_path(imp_qf_clearing) %></td>
        <td><%= link_to 'Destroy', imp_qf_clearing, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Imp qf clearing', new_imp_qf_clearing_path %>
