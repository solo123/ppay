<%= form_for(@imp_qf_clearing) do |f| %>
  <% if @imp_qf_clearing.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@imp_qf_clearing.errors.count, "error") %> prohibited this imp_qf_clearing from being saved:</h2>

      <ul>
      <% @imp_qf_clearing.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
