<%= form_for(@imp_qf_trade) do |f| %>
  <% if @imp_qf_trade.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@imp_qf_trade.errors.count, "error") %> prohibited this imp_qf_trade from being saved:</h2>

      <ul>
      <% @imp_qf_trade.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
